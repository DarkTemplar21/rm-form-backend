ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.richmeat.ApplicationKt.module ]
    }
    db {
        jdbcUrl = ${172.16.10.238}
        dbUser = ${root}
        dbPassword = ${root}
    }
}
